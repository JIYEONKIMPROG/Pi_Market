import { PayloadTransport } from '@magic-sdk/provider';
export declare class WebTransport extends PayloadTransport {
    /**
     * Initialize the underlying `Window.onmessage` event listener.
     */
    protected init(): void;
}
