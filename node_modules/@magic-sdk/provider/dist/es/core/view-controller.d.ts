import { MagicMessageRequest } from '@magic-sdk/types';
import { PayloadTransport } from './payload-transport';
export declare abstract class ViewController<Transport extends PayloadTransport = PayloadTransport> {
    protected readonly transport: Transport;
    ready: Promise<void>;
    protected readonly endpoint: string;
    protected readonly parameters: string;
    constructor(transport: Transport);
    protected abstract init(): void;
    abstract postMessage(data: MagicMessageRequest): Promise<void>;
    protected abstract hideOverlay(): void;
    protected abstract showOverlay(): void;
    private waitForReady;
    /**
     * Listen for messages sent from the underlying Magic `<WebView>`.
     */
    private listen;
}
